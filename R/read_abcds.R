read_abcds <- function(directory,
                       participant = TRUE,
                       control = FALSE,
                       add_demographics = c(""),
                       entire_data = FALSE,
                       ...) # ... is variable names
{
  
}